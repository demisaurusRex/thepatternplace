<div class="container-fluid">
  <h1>New review</h1>
  <%= simple_form_for [@pattern, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit "Submit review", class: "btn btn-dark" %>
    <%= link_to 'exit', pattern_path(@pattern), class: 'btn btn-outline-dark' %>

  <% end %>
</div>
